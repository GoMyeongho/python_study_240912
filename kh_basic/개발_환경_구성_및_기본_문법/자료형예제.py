# 문자열(문자 타입은 존재하지 않음), 숫자(정수, 실수), Boolean(참, 거짓)
# 변수란? 자료형을 이용해 실제 데이터를 저장할 공간을 할당 받는 것
# 변수의 값을 대입 하는 연산자 '='

a = b = c =1 # 뒤에서 부터 앞으로 대입
a, b, c = 1, True, "곰돌이" # 순서에 맞게 하나하나 대입

# 변수의 타입 확인 : type()
print(type(a))
print(type(b))
print(type(c))

































